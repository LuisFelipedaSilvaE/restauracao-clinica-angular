import { IconColor } from './icon-color';

describe('IconColor', () => {
  it('should create an instance', () => {
    const directive = new IconColor();
    expect(directive).toBeTruthy();
  });
});
