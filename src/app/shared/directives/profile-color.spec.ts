import { ProfileColor } from './profile-color';

describe('ProfileColor', () => {
  it('should create an instance', () => {
    const directive = new ProfileColor();
    expect(directive).toBeTruthy();
  });
});
