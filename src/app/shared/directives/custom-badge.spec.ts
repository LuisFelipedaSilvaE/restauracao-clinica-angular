import { CustomBadge } from './custom-badge';

describe('CustomBadge', () => {
  it('should create an instance', () => {
    const directive = new CustomBadge();
    expect(directive).toBeTruthy();
  });
});
