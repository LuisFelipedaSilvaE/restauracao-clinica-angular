import { CustomProgressbar } from './custom-progressbar';

describe('CustomProgressbar', () => {
  it('should create an instance', () => {
    const directive = new CustomProgressbar();
    expect(directive).toBeTruthy();
  });
});
